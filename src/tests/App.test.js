// import React from 'react';
// import App from '../App';
// import categoriesListDrinksApi from '../services/categoriesListDrinksApi';
// import { renderWithRouterAndRedux } from './helpers/renderWith';

describe('Testes de exceções nas Api"s:', () => {
  describe('1. Testa serviço categoriesListDrinksApi.', () => {
    // beforeEach(() => {
    //   jest.spyOn(global, 'fetch').mockImplementation('error');
    // });da

    // afterEach(() => {
    //   global.fetch.mockClear();
    // });
    test('1.1. Testa se houver error na resposta da Api.', async () => {
      // const { history } = renderWithRouterAndRedux(
      //   <App />,
      //   { initialEntries: ['/meals'] },
      // );
      // try {
      //   await categoriesListDrinksApi();
      // } catch (e) {
      //   expect(e).toMatch('error');
      // }
      // expect(await categoriesListDrinksApi()).rejects.toMatch('error');
    });
  });
});
